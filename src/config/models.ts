/**
 * Model field lookup codes — must match services/dff-service `modules/models/constants.ts`.
 * Option labels and creatable statuses come from GET /api/v1/models/config + lookup APIs.
 */
export const MODEL_STATUS_LOOKUP = "RECORD_STATUS";

export const MODEL_FRAMEWORK_TYPE_LOOKUP = "MODEL_FRAMEWORK_TYPE";
